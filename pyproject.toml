[tool.ruff]
line-length = 120
target-version = "py310"
extend-exclude = ["training/*.ipynb", ".venv-check"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
