av==17.1.0
numpy==2.2.6
Pillow==12.3.0
streamlit==1.64.0
streamlit-webrtc==0.78.1
# torch/torchvision are left unpinned on purpose: install the CUDA build first
# (see README) if you have an NVIDIA GPU, then this file will leave it alone.
torch
torchvision
ultralytics==8.4.157
opencv-python-headless==5.0.0.93
requests==2.34.2
streamlit-geolocation==0.0.10
piexif==1.1.3
fpdf2==2.8.8

# Transitive deps pulled in below their fixed version by other packages above;
# pinned explicitly to clear known CVEs flagged by the container security scan.
setuptools>=78.1.1  # CVE-2025-47273
msgpack>=1.2.1      # GHSA-6v7p-g79w-8964
